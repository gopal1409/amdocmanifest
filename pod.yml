apiVersion: v1
kind: Pod
metadata:
  name: static-web
  labels:
    app: mynginx

spec:
  containers:
    - name: web
      image: nginx
      ports:
        - name: web
          containerPort: 80
          